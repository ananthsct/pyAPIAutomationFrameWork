pytest -v -s
allure serve allure-results   #to check allure-commandline installed
pytest -s -v tests/* --alluredir=./reports --html=report.html
pytest -s -v tests/integration_test/test_create_booking.py --html=report.html

git init
git add .          #add directories to be ignored under .gitignore
git push origin master
SKIP=gitleaks git commit -m "first commit"   #to avoid gitleaks error
